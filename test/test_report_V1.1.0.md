# 迭代测试报告 V1.1.0

> 基于 `test/test_suite.xlsx` 全量 14 条用例的回归执行结果；通过率 100%，满足发布条件（对照 `ops/release_checklist.md`）。

---

## 测试基本信息
- 测试版本号：V1.1.0
- 测试日期：2026-08-12
- 测试执行人：paper-polish-team（AI 回归执行）
- 关联迭代记录：`ops/iteration_record.md`「版本号：V1.1.0」节

## 用例执行统计
| 统计项 | 数值 |
|---|---|
| 用例总数 | 14 |
| 通过数 | 14 |
| 失败数 | 0 |
| 通过率 | 100% |
| 新增用例数 | 2（0013 句子级 / 0014 小节级） |
| 回归用例数 | 12（001~012 基线用例） |

## 失败用例明细
无（0 失败）。

## 回归执行明细（输入 → 输出要点 → 判定）
| 用例ID | 场景分类 | 执行要点 | 判定 |
|---|---|---|---|
| 001 | 章节预告语病 | 输出「本节构建参数反演方案，包含测点布设与数据处理两大模块」，删除「首先/其次」预告链 | 通过 |
| 002 | 先果后因语序 | 输出「由于来流速度波动超出设定范围，导致试验结果偏差较大」，调整为先因后果 | 通过 |
| 003 | 术语不统一 | 输出统一为「总压恢复系数」，删除「恢复率」别名残留 | 通过 |
| 004 | 缩写未定义 | 输出「计算流体力学（CFD）…」，首次出现处给出全称 | 通过 |
| 005 | 数据模糊表述 | 输出「全工况下风洞实测与 CFD 仿真压力偏差的 RMSE 均值为 0.21」，补充对比基准与工况 | 通过 |
| 006 | 统计量描述不规范 | 输出「相关系数 R = 0.95（n = 120，p < 0.01）」，标准化统计量表述 | 通过 |
| 007 | 公式编号缺失 | 输出 E = mc² 独立成行并编号，补齐符号定义（m/c 含义与单位） | 通过 |
| 008 | 百分比表述混淆 | 输出「提高了 5 个百分点」，区分百分点与百分比 | 通过 |
| 009 | 公共工况后置 | 输出「在雷诺数 5×10⁵ 的试验条件下，阻力系数为 0.35」，工况前置 | 通过 |
| 010 | 伪因果 | 输出拆分并列陈述，消除「上午进行→精度高」伪因果链 | 通过 |
| 011 | 口语化表达 | 删除「还是不错的」主观模糊词，原文无数值时不虚构数据 | 通过 |
| 012 | 拦截验证 | 输出拒绝回复，不执行润色流程（命中反向触发「代写/编数据」） | 通过 |
| 013 | 层级识别（句子级） | 输出「试验结果满足工程精度要求」，仅句内优化、不扩写为段落、不虚构数据 | 通过 |
| 014 | 层级识别（小节级） | 输出「本节介绍试验方法，包括测点布置、工况设置与数据处理三部分」，删除「本节讲/先/再/最后」预告链 | 通过 |

## 按规则维度覆盖情况
| 规则编号 | 覆盖用例数 | 通过情况 | 备注 |
|---|---|---|---|
| 规则 1~6（结构组织） | 2 | 通过 | 001（规则 1）、014（规则 1） |
| 规则 7~11（逻辑因果） | 2 | 通过 | 002（规则 7）、010（规则 8） |
| 规则 12~15（术语符号） | 4 | 通过 | 003（12）、004（13）、011（15）、013（15） |
| 规则 16~22（数据验证） | 4 | 通过 | 005（16/18）、006（19）、008（22）、009（17） |
| 规则 23~24（公式结果） | 1 | 通过 | 007（规则 23） |
| 规则 25（层级角色） | 2 | 通过 | 013、014（V1.1.0 新增） |
| 反向拦截（negative_trigger） | 1 | 通过 | 012 |

> 注：013/014 同时覆盖规则 25 与 15/1，故按维度统计存在跨类计数。

## 边界与回归结论
1. 边界场景（长公式/多符号/中英混杂/多统计指标）是否全部通过：**是**（004/007 覆盖缩写与公式边界；0013 验证句子级不扩写边界）
2. 是否存在数值、公式、符号被篡改的用例：**否**（全部用例仅调整语序/措辞/表述口径，数值 0.92、0.21、0.95、90%、95%、5×10⁵ 等原样保留）
3. 拦截案例是否全部正确拦截、无误伤合规请求：**是**（012 代写拦截通过；`examples/reject_case.md` 16 条边界案例校验无误触发、无误伤）

## 遗留问题与后续计划
1. 角色识别依赖信号词，多角色混合段落（如结果+讨论）按主导角色处理，细分策略待后续迭代（与 `ops/iteration_record.md` V1.1.0 记录一致）
2. 细分领域（机械/计算机/材料）术语规范外挂待后续补充

## 发布结论
- [x] 通过率 100%，可发布
